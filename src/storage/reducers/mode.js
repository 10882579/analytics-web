const initialState  = {
  SERVER: "production",
  API_URI: "https://analyticsapi.herokuapp.com"
}



const reducer = (state = initialState, action) => {
  switch (action.type) {

      
  }
  return state
}

export default reducer
